{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the main window. Deliberately not the permissive Tauri starter set — every permission added here must be justified by a feature that needs it.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
